<ul class="tc-ctl-lcat-node" id="{{id}}">
    <span>{{title}}</span>
    <ul class="tc-ctl-lcat-branch tc-collapsed">

        {{#each children}}
            {{> tc-ctl-lcat-node}}
        {{/each}}

    </ul>
</ul>