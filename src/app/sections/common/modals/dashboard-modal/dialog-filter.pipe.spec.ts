import { DialogFilterPipe } from './dialog-filter.pipe';

describe('DialogFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DialogFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
