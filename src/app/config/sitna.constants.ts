/**
 * Central constants for SITNA/map integration. Reduces magic strings in app code.
 */

/** DOM id of the map container element (must match markup and layout). */
export const MAP_CONTAINER_ID = 'mapa';
